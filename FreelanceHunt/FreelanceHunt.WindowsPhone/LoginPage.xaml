<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FreelanceHunt"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity" xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    x:Class="FreelanceHunt.LoginPage"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" DataContext="{Binding LoginViewModel, Mode=OneWay, Source={StaticResource Locator}}">

    <Grid>
        <StackPanel VerticalAlignment="Center" Margin="15,0">
            <TextBlock Text="Авторизация" Style="{StaticResource HeaderTextBlockStyle}"/>
            <TextBox PlaceholderText="Введите секретный ключ" Text="{Binding Login, Mode=TwoWay}" />
            <TextBox PlaceholderText="Введите токен" Text="{Binding Password, Mode=TwoWay}"/>
            <Button Content="Войти" HorizontalAlignment="Stretch">
                <Interactivity:Interaction.Behaviors>
                    <Core:EventTriggerBehavior EventName="Click">
                        <Core:InvokeCommandAction Command="{Binding LoginCommand, Mode=OneWay}"/>
                    </Core:EventTriggerBehavior>
                </Interactivity:Interaction.Behaviors>
            </Button>
        </StackPanel>
    </Grid>
</Page>
